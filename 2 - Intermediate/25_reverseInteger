// reverse an integer
function reverseInt(num) {
  // convert it to the string, then to array, reverse, and join
  let str = num.toString().split('').reverse().join('');
  let result = parseInt(str);
  return result;
}

console.log(reverseInt(73));
