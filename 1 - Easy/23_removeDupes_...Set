// remove duplicates
function removeDupes(arr) {
  // using brackets around it turns it into an array, instead of an object
  let newArr = [...new Set(arr)];
  return newArr;
}

console.log(removeDupes([1,2,3,3,3]));
