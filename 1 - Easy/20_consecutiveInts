// function that returns if there are two consecutive ints in an arr
function consecutiveInts(arr) {
  // loop through the array
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === arr[i + 1]) {
      return true;
    } else {
    }
  }
  return false;
}
